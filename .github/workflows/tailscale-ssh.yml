name: tailscale ssh
on:
  workflow_dispatch:

jobs:
  ssh:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - uses: tailscale/github-action@main
        with:
          authkey: ${{ secrets.TAILSCALE_AUTH_KEY }}
          args: --ssh

      - run: sleep infinity
